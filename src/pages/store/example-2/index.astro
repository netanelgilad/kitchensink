---
// Redirect to the all-products category page
const searchParams = Astro.url.search;
const redirectUrl = `/store/example-2/category/all-products${searchParams}`;
return Astro.redirect(redirectUrl);
---
